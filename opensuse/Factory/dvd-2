#INCLUDE common-dvd5

job lock name novell-ipsec-tools
job lock name myspell-german
job lock name acroread_ja
job lock name apache2-prefork

# there is a flip going on between dvd-1 and dvd-2
job lock name postgresql

job install name sendmail
job install name atftp
job install name squid
job install name icewm-default
job lock name libicu50-32bit # !ppc64le # !aarch64

job install provides pattern() = x11_yast

