namespace namespace:language(en) @SYSTEM
namespace namespace:language(de) @SYSTEM

#INCLUDE all-supplements.i586 # !x86_64
#INCLUDE all-supplements.x86_64 # !i586

job lock name oxygen-icon-theme-scalable
job lock name spamassassin
job lock name gutenprint
job lock name rdesktop
job lock name gimp
job lock name ufraw

# SLE themes 
job install name branding-openSUSE
job lock name gnome-menus-branding-SLED
job lock name gtk2-branding-SLED
job lock name gtk2-theme-SLED

#INCLUDE ignore_opensuse-bundle-gnome
job lock name compiz-kde
job lock name gtk2-engines-32bit
job lock name kde3-i18n-de
job lock name vim-data
job lock name java-1_7_0-openjdk
job lock name gimp
job lock name netpbm

job lock name gnome-icon-theme-extras
job lock name gnome-icon-theme-symbolic
job lock name patterns-openSUSE-yast2_basis

job lock name PackageKit
job lock name zeitgeist

job lock name ghostscript
job lock name cups
job lock name pullin-flash-player
job lock name pullin-fluendo-mp3
job lock name patterns-openSUSE-apparmor
job install name yast2-country
job install name yast2-trans-stats
job lock name python3-base
job lock name samba-libs

# live-installer has been dropped - 2016-05-17 - dimstar
#job install name yast2-live-installer

# the live isos are still based on gfxboot
job install name gfxboot

#INCLUDE ignore_opensuse
#INCLUDE ignore_opensuse-basedon
#INCLUDE ignore_opensuse-cds
job install provides pattern() = rest_cd_x11
job install name openSUSE-release-livecd-x11

job lock name MozillaFirefox-translations-common
job lock name libjavascriptcoregtk-4_0-18

# We need space - dimstar - 2018-06-18
job lock name ghostscript-fonts-std
# The Rescue CD is not installable - there is no need to PW check
job lock name cracklib-dict-full

# Install yast2 branding until sr#411752 is accepted
job install name yast2-qt-branding-openSUSE

