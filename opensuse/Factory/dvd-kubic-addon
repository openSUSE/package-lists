# No recommended packages on the DVD
solverflags ignorerecommended

# We can't use ignore_opensuse, as that also ignores openSUSE-Kubic-release
job lock name libudev-mini1

job install provides pattern() = SUSE%2dCaaSP%2donlyDVD

job install name openSUSE-Tumbleweed-Kubic-release
job install name systemd-presets-branding-CAASP
job lock name systemd-presets-branding-openSUSE


