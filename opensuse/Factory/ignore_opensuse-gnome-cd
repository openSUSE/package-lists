job lock name opensuse-manuals_en
job lock name PolicyKit-doc

#job lock name tomboy-lang
#job lock name beagle-lang
#job lock name gnome-do-lang

job lock name python-opengl
job lock name smolt-gui
job lock name esound-daemon

job lock name libreoffice-draw
job lock name libreoffice-impress
job lock name libreoffice-icon-theme-andromeda
job lock name libreoffice-icon-theme-crystal
job lock name libreoffice-icon-theme-breeze
# Tango icon theme now depends on -galaxy
#job lock name libreoffice-icon-theme-galaxy
job lock name libreoffice-icon-theme-sifr
job lock name libreoffice-icon-theme-hicontrast
job lock name libreoffice-icon-theme-industrial
job lock name libreoffice-icon-theme-oxygen
job lock name shotwell

job lock name tango-icon-theme

# shrink the live CD - pattern change pending
job lock name colord
job lock name tk
# deja-dup was added to fill up the live CDs.. now they are full (pattern change pending)
job lock name deja-dup

# GNOME relies on webkit1 GTK3 and Webkit2 GTK3... let's not add webkit2 GTK2
job lock name libwebkitgtk-1_0-0

job lock name brasero
job lock name libreoffice-calc

# sigh - a new kernel, again more space needed (20170226)
job lock name apparmor-utils

# Can we get rid of webkit1 on the live? (20170312)
job lock name libwebkitgtk-3_0-0

# no one needed
job lock name gdk-pixbuf-lang

