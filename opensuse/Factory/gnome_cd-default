namespace namespace:language(en) @SYSTEM

#INCLUDE ignore_opensuse
#INCLUDE ignore_opensuse-cds
#INCLUDE ignore_opensuse-gnome-cd
#INCLUDE ignore_opensuse-images
#INCLUDE ignore_opensuse-basedon
job lock name gnome2-user-docs
job lock name mozilla-xulrunner192
job lock name gtk2-32bit

job install name branding-openSUSE
job install name kernel-default
job install name openSUSE-release-ftp
job install provides pattern() = gnome

job lock name patterns-openSUSE-gnome_utilities
job lock name patterns-openSUSE-office

# get openSUSE-images under control 
job lock name evolution
job lock name aisleriot
job lock name kernel-firmware
job lock name tcl

job lock name gnome-chess
job lock name brltty

# We urgently need space
job lock name libjavascriptcoregtk-3_0-0
